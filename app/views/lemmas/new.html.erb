<h1>New lemma</h1>

<% form_for(@lemma) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :lemma %><br />
    <%= f.text_field :lemma %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', lemmas_path %>